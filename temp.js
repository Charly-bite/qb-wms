const sql = require("mssql"); const config = { server: "192.168.2.237", user: "sga_app_user", password: "QuimicaBoss_2026!", database: "SGA_Database", options: { encrypt: false, trustServerCertificate: false } }; async function run() { try { await sql.connect(config); const result = await sql.query("SELECT TOP 5 product_id, sap_matnr, chemical_name FROM products_master WHERE product_id LIKE '%IFF%'"); console.log(result.recordset); } catch (err) { console.error(err); } finally { process.exit(); } }; run();
